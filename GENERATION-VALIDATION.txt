PHANTOM 2C-4 GENERATION VALIDATION

Static checks performed:
- changed Rust delimiters balanced;
- no todo!, unimplemented!, panic!, dbg!, .unwrap() or .expect() introduced;
- no quality gate disabled;
- codec types remain inside phantom-image;
- animation frames are not stored in DOM/Layout/Paint;
- frame and aggregate memory limits are explicit;
- GIF and animated-WebP deterministic fixtures included.

Authoritative validation remains the user's Rust 1.95 workspace.
